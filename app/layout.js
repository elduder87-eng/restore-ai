export const metadata = {
  title: "Restore AI",
  description: "Restore AI — Teacher Mode",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
